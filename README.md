# Global Enterprises — Landing Page

Landing page for Global Enterprises, a cycle / tyre / battery / car-key
shop in Marathahalli, Bengaluru.

Plain HTML/CSS/JS, no build step — `index.html` is the entry point.

## Local preview

```
python3 -m http.server 8000
```

Then open http://localhost:8000
